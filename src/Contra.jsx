import React from 'react'


const Contra = () => {
  return (
    {/*
    <div className='p-5'>
        <h2>Restablecer Contraseña</h2>
        {message && <div>{message}</div>}
        {error && <div>{error}</div>}
        <form onSubmit={handleResetPassword}>
          <div>
            <label  className='p-4' htmlFor="email">Correo Electrónico:</label>
            
            <input
           
              type="email"
              id="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
            />
          </div>
          <button className='ms-4 css-button css-button-rounded css-button-rounded--blue' type="submit">Enviar Correo Electrónico</button>
        </form>
      </div>
        */}
  )
}

export default Contra
